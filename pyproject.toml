[project]
name = "freewubi"
version = "0.1.0"
description = "Wubi 86 input method engine for Fcitx5 on Linux"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
