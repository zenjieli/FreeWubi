# Revisions that only reformat code (e.g. clang-format rollout).
# git blame skips these so the blame still points at real changes.
# Enable locally with:
#   git config blame.ignoreRevsFile .git-blame-ignore-revs

# Apply clang-format (Google style, 120 columns)
5d8fe609eab3bcd3a4c4849d60bb5e852406e707

# Format test_engine.cpp (missed by the initial rollout) + extend CI to tests/
a807e9e0829c478952ea3e001f9e3e1bfd5031f5
